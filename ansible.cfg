# リポジトリ共通のAnsible設定。
# リポジトリ直下から ansible-playbook を実行する前提
# (AWXでもプロジェクトルートのこのファイルが読み込まれる)。
[defaults]
# ロールはすべて ./roles に集約している(共通処理・Proxmox操作・サービス構築)
roles_path = ./roles
# 既定のインベントリはlab環境。他環境を追加したら -i inventories/<環境名> で切り替える
inventory = ./inventories/lab
localhost_warning = False
