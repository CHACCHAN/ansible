---
- name: 必須変数が正しく渡されているか検証する
  ansible.builtin.assert:
    that:
      - template_id is defined
      - vm_id is defined
      - vm_name is defined
      - target_storage is defined
    fail_msg: >-
      必須変数が不足しています。
    quiet: true
